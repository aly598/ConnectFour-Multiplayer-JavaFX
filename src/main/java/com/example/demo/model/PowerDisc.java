package com.example.demo.model;

/** Power disc options that a player can attach to a drop. */
public enum PowerDisc {
    NONE,
    CLEAR_COLUMN, // clears the chosen column instead of dropping
    DOUBLE_POINTS // counts winning sequence as worth 2x in score table
}
